/**
 * Theme — dark/light palettes, spacing. Phase 4: light mode + useTheme.
 * Now uses design system tokens for consistency.
 */

// Re-export design system tokens for backward compatibility
export { spacing, darkColors, lightColors, colors, type ColorPalette } from '../design-system/tokens';
